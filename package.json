{
  "name": "identity-studio",
  "version": "2.1.1",
  "private": true,
  "type": "module",
  "description": "An identity studio for your next AI employee.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "npm run check",
    "start": "node scripts/serve.mjs",
    "dev": "node scripts/serve.mjs",
    "check": "node --check scripts/serve.mjs && node --check dist/app.js && node --check dist/identity.js && node --check dist/image-validation.js && node --check dist/avatar.js && node --check dist/avatar-editor.js && node --test tests/*.test.mjs"
  }
}
